{
  "name": "thanatsitt-portfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "live-server . --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
